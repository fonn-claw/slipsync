---
phase: 01-foundation-data-layer
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(admin)/layout.tsx
  - src/app/(admin)/dashboard/page.tsx
  - src/app/(staff)/layout.tsx
  - src/app/(staff)/operations/page.tsx
  - src/app/(boater)/layout.tsx
  - src/app/(boater)/my-bookings/page.tsx
  - src/components/layouts/admin-sidebar.tsx
  - src/components/layouts/staff-sidebar.tsx
  - src/components/layouts/boater-navbar.tsx
  - src/components/layouts/marina-logo.tsx
  - src/components/layouts/logout-button.tsx
autonomous: false
requirements:
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "Application renders with navy/teal/seafoam nautical color palette -- not default shadcn/ui zinc"
    - "Admin sees a deep navy sidebar with marina branding, navigation links, and logout button"
    - "Staff sees a deep navy sidebar with dock operations navigation and logout button"
    - "Boater sees a top navigation bar with horizontal links and logout button"
    - "Sidebar collapses on tablet screens (below 1024px)"
    - "shadcn/ui components (buttons, inputs, cards) use the nautical theme colors"
    - "Login page is styled with nautical theme and shows marina branding"
  artifacts:
    - path: "src/app/globals.css"
      provides: "OKLCH nautical theme CSS custom properties"
      contains: "--primary: oklch"
    - path: "src/components/layouts/admin-sidebar.tsx"
      provides: "Admin sidebar navigation component"
    - path: "src/components/layouts/staff-sidebar.tsx"
      provides: "Staff sidebar navigation component"
    - path: "src/components/layouts/boater-navbar.tsx"
      provides: "Boater top navigation component"
    - path: "src/components/layouts/marina-logo.tsx"
      provides: "Shared marina branding component with anchor/wave icon"
  key_links:
    - from: "src/app/(admin)/layout.tsx"
      to: "src/components/layouts/admin-sidebar.tsx"
      via: "imports and renders AdminSidebar"
      pattern: "import.*AdminSidebar"
    - from: "src/app/(boater)/layout.tsx"
      to: "src/components/layouts/boater-navbar.tsx"
      via: "imports and renders BoaterNavbar"
      pattern: "import.*BoaterNavbar"
    - from: "src/app/globals.css"
      to: "shadcn/ui components"
      via: "CSS custom properties consumed by all shadcn components"
      pattern: "--primary:"
---

<objective>
Apply the ocean/nautical theme and build role-specific layout shells with professional navigation using shadcn/ui components.

Purpose: Transform the bare application into a visually impressive, LinkedIn-ready marina management interface. Each role gets a tailored navigation experience that establishes the information architecture for all subsequent phases.
Output: Nautical-themed application with admin sidebar, staff sidebar, boater top nav, styled login page, and responsive layouts.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md

<interfaces>
<!-- From Plan 02 outputs -->
From src/lib/auth.ts:
```typescript
export interface SessionData {
  userId: number;
  email: string;
  name: string;
  role: 'admin' | 'dock_staff' | 'boater';
  isLoggedIn: boolean;
}
export async function getSession(): Promise<IronSession<SessionData>>;
```

From src/app/(auth)/login/actions.ts:
```typescript
export async function loginAction(prevState: any, formData: FormData): Promise<{error: string} | void>;
export async function logoutAction(): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui components and apply nautical OKLCH theme to globals.css</name>
  <files>
    src/app/globals.css, src/app/layout.tsx
  </files>
  <read_first>
    src/app/globals.css
    src/app/layout.tsx
    .planning/phases/01-foundation-data-layer/01-RESEARCH.md
    package.json
  </read_first>
  <action>
    1. Initialize shadcn/ui if not already done:
       ```
       npx shadcn@latest init --defaults
       ```
       If prompted, accept defaults (New York style, etc.).

    2. Add required shadcn/ui components:
       ```
       npx shadcn@latest add button card input label form sidebar sheet avatar dropdown-menu separator badge scroll-area tooltip
       ```

    3. Replace `src/app/globals.css` with the FULL nautical OKLCH theme from RESEARCH.md Pattern 4. The file must contain:
       - `@import "tailwindcss";` at the top
       - `:root` block with ALL these CSS custom properties (exact OKLCH values from research):
         - `--background: oklch(0.98 0.005 230)`
         - `--foreground: oklch(0.15 0.03 240)`
         - `--card: oklch(1 0 0)`
         - `--card-foreground: oklch(0.15 0.03 240)`
         - `--popover: oklch(1 0 0)`
         - `--popover-foreground: oklch(0.15 0.03 240)`
         - `--primary: oklch(0.45 0.12 220)`
         - `--primary-foreground: oklch(0.98 0.005 230)`
         - `--secondary: oklch(0.92 0.04 180)`
         - `--secondary-foreground: oklch(0.25 0.06 220)`
         - `--muted: oklch(0.94 0.02 200)`
         - `--muted-foreground: oklch(0.45 0.03 220)`
         - `--accent: oklch(0.88 0.06 180)`
         - `--accent-foreground: oklch(0.20 0.06 220)`
         - `--destructive: oklch(0.55 0.2 25)`
         - `--destructive-foreground: oklch(0.98 0 0)`
         - `--border: oklch(0.88 0.02 220)`
         - `--input: oklch(0.88 0.02 220)`
         - `--ring: oklch(0.45 0.12 220)`
         - `--radius: 0.625rem`
         - ALL sidebar variables: `--sidebar: oklch(0.20 0.05 240)`, `--sidebar-foreground: oklch(0.92 0.02 220)`, `--sidebar-primary: oklch(0.75 0.10 180)`, `--sidebar-primary-foreground: oklch(0.15 0.03 240)`, `--sidebar-accent: oklch(0.28 0.04 240)`, `--sidebar-accent-foreground: oklch(0.92 0.02 220)`, `--sidebar-border: oklch(0.30 0.04 240)`, `--sidebar-ring: oklch(0.45 0.12 220)`
         - ALL chart colors: `--chart-1` through `--chart-5`
       - `@theme inline` block mapping all CSS vars to Tailwind color tokens (--color-background, --color-foreground, etc.) exactly as shown in RESEARCH.md
       - Inter font import (Google Fonts or next/font) and body font-family set to Inter

    4. Update `src/app/layout.tsx`:
       - Import Inter from `next/font/google`
       - Set metadata: title="SlipSync - Marina Management", description="Modern marina slip booking and management"
       - Apply Inter font className to html element
       - Minimal root layout: just html, body with `{children}`. NO padding, NO structural layout. Route group layouts handle structure.
       - Add `suppressHydrationWarning` to html if needed for next-themes compatibility

    5. Verify `npm run build` passes with the new theme.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains `--primary: oklch(0.45 0.12 220)`
    - src/app/globals.css contains `--sidebar: oklch(0.20 0.05 240)`
    - src/app/globals.css contains `--background: oklch(0.98 0.005 230)`
    - src/app/globals.css contains `@theme inline`
    - src/app/globals.css contains `--color-primary: var(--primary)`
    - src/app/globals.css contains `@import "tailwindcss"`
    - src/app/layout.tsx contains `Inter`
    - src/app/layout.tsx contains `SlipSync`
    - src/components/ui/button.tsx exists (shadcn button installed)
    - src/components/ui/card.tsx exists (shadcn card installed)
    - src/components/ui/sidebar.tsx exists (shadcn sidebar installed)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Nautical OKLCH theme applied to globals.css. All shadcn/ui components installed and themed. Root layout is minimal with Inter font. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build role-specific layout shells with sidebar/navbar navigation and style login page</name>
  <files>
    src/components/layouts/marina-logo.tsx,
    src/components/layouts/admin-sidebar.tsx,
    src/components/layouts/staff-sidebar.tsx,
    src/components/layouts/boater-navbar.tsx,
    src/components/layouts/logout-button.tsx,
    src/app/(admin)/layout.tsx,
    src/app/(admin)/dashboard/page.tsx,
    src/app/(staff)/layout.tsx,
    src/app/(staff)/operations/page.tsx,
    src/app/(boater)/layout.tsx,
    src/app/(boater)/my-bookings/page.tsx,
    src/app/(auth)/login/page.tsx
  </files>
  <read_first>
    src/app/globals.css
    src/app/layout.tsx
    src/lib/auth.ts
    src/app/(auth)/login/actions.ts
    src/app/(auth)/login/page.tsx
    src/app/(admin)/layout.tsx
    src/app/(staff)/layout.tsx
    src/app/(boater)/layout.tsx
    src/components/ui/sidebar.tsx
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    src/components/ui/sheet.tsx
  </read_first>
  <action>
    1. Create `src/components/layouts/marina-logo.tsx`:
       - Shared branding component used in all sidebars and navbars
       - Render an Anchor icon (from lucide-react) + "SlipSync" text
       - Accept `collapsed` prop to show only icon when sidebar is collapsed
       - Use primary/sidebar color tokens for styling
       - Professional typography: "SlipSync" in semibold, small "Marina Management" subtitle text

    2. Create `src/components/layouts/logout-button.tsx`:
       - Client component that calls logoutAction from login/actions.ts
       - Renders a button/dropdown item with LogOut icon (lucide-react)
       - Can be used in sidebar footer or navbar

    3. Create `src/components/layouts/admin-sidebar.tsx`:
       - Use shadcn/ui Sidebar component (SidebarProvider, Sidebar, SidebarHeader, SidebarContent, SidebarGroup, SidebarMenu, SidebarMenuItem, SidebarMenuButton, SidebarFooter)
       - Header: MarinaLogo component
       - Navigation items with lucide-react icons:
         - LayoutDashboard icon: "Dashboard" -> /admin/dashboard
         - Map icon: "Marina Map" -> /admin/marina-map (placeholder for Phase 2)
         - Calendar icon: "Bookings" -> /admin/bookings (placeholder for Phase 3)
         - Users icon: "Boaters" -> /admin/boaters (placeholder for Phase 4)
         - Wrench icon: "Maintenance" -> /admin/maintenance (placeholder for Phase 4)
         - ListOrdered icon: "Waitlist" -> /admin/waitlist (placeholder for Phase 4)
       - Footer: User avatar/name display + LogoutButton
       - Active state: highlight current route using `usePathname()`
       - Deep navy background using sidebar CSS tokens (--sidebar, --sidebar-foreground, etc.)
       - Collapsible on tablet (use SidebarProvider's collapsible prop)

    4. Create `src/components/layouts/staff-sidebar.tsx`:
       - Same structure as admin sidebar but with dock staff navigation:
         - Map icon: "Marina Map" -> /staff/operations (will be marina map in Phase 2)
         - Ship icon: "Check In/Out" -> /staff/check-in (placeholder)
         - Wrench icon: "Maintenance" -> /staff/maintenance (placeholder)
         - Calendar icon: "Schedule" -> /staff/schedule (placeholder)
       - Footer: User name + LogoutButton
       - Same deep navy styling as admin sidebar

    5. Create `src/components/layouts/boater-navbar.tsx`:
       - Horizontal top navigation bar (NOT a sidebar)
       - Left: MarinaLogo (horizontal layout)
       - Center/Right: Navigation links:
         - "My Bookings" -> /boater/my-bookings
         - "Browse Slips" -> /boater/browse (placeholder)
         - "My Vessels" -> /boater/vessels (placeholder)
         - "Maintenance" -> /boater/maintenance (placeholder)
       - Far right: User name/avatar + LogoutButton dropdown
       - Use background color from theme (white/light card background)
       - Active link highlighted with primary/accent color
       - On mobile: hamburger menu using shadcn/ui Sheet component

    6. Update `src/app/(admin)/layout.tsx`:
       - Server component that reads session via getSession() to get user name/role
       - Wraps children with SidebarProvider + AdminSidebar
       - Layout: sidebar on left, main content area on right with proper padding (p-6)
       - Content area has a max-width container for readability

    7. Update `src/app/(staff)/layout.tsx`:
       - Same pattern as admin layout but with StaffSidebar
       - Server component reading session for user info

    8. Update `src/app/(boater)/layout.tsx`:
       - Server component reading session for user info
       - BoaterNavbar at top, content below with padding (p-6)
       - Full-width content area (no sidebar)

    9. Update `src/app/(admin)/dashboard/page.tsx`:
       - Styled placeholder: "Dashboard" heading in h1
       - Card component with "Coming in Phase 4" message
       - Show a few stat placeholder cards (Total Slips: 60, Occupancy: --, Revenue: --)
       - Use shadcn/ui Card, Badge components

    10. Update `src/app/(staff)/operations/page.tsx`:
        - Styled placeholder: "Marina Operations" heading
        - Card: "Marina Map coming in Phase 2"
        - Professional empty state with Map icon

    11. Update `src/app/(boater)/my-bookings/page.tsx`:
        - Styled placeholder: "My Bookings" heading
        - Card: "Booking management coming in Phase 3"
        - Professional empty state with Calendar icon

    12. Restyle `src/app/(auth)/login/page.tsx`:
        - Center the login form vertically and horizontally
        - Use shadcn/ui Card for the form container
        - MarinaLogo at top of card (large, prominent)
        - "Welcome to SlipSync" heading, "Sign in to manage your marina" subtitle
        - Styled form inputs using shadcn/ui Input, Label components
        - Primary-colored "Sign In" Button (full width)
        - Demo credentials section below form in muted text: show all 3 accounts
        - Subtle ocean-themed background: use the muted/background gradient or a wave pattern
        - Error message displayed in destructive color with an alert icon

    13. Run `npm run build` to verify everything compiles.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layouts/marina-logo.tsx contains `Anchor` (from lucide-react) and `SlipSync`
    - src/components/layouts/admin-sidebar.tsx contains `SidebarProvider` or `Sidebar`
    - src/components/layouts/admin-sidebar.tsx contains `Dashboard`
    - src/components/layouts/admin-sidebar.tsx contains `/admin/dashboard`
    - src/components/layouts/staff-sidebar.tsx contains `Sidebar`
    - src/components/layouts/staff-sidebar.tsx contains `/staff/operations`
    - src/components/layouts/boater-navbar.tsx contains `My Bookings`
    - src/components/layouts/boater-navbar.tsx contains `/boater/my-bookings`
    - src/components/layouts/logout-button.tsx contains `logoutAction`
    - src/app/(admin)/layout.tsx contains `AdminSidebar` or `admin-sidebar`
    - src/app/(admin)/layout.tsx contains `getSession`
    - src/app/(staff)/layout.tsx contains `StaffSidebar` or `staff-sidebar`
    - src/app/(boater)/layout.tsx contains `BoaterNavbar` or `boater-navbar`
    - src/app/(auth)/login/page.tsx contains `Card`
    - src/app/(auth)/login/page.tsx contains `SlipSync` or `MarinaLogo`
    - src/app/(admin)/dashboard/page.tsx contains `Dashboard`
    - src/app/(staff)/operations/page.tsx contains `Operations` or `Marina`
    - src/app/(boater)/my-bookings/page.tsx contains `Bookings`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Three role-specific layouts with professional navigation render correctly. Admin and staff have deep navy sidebars with collapsible behavior. Boater has a top navigation bar. Login page is styled with nautical theme and marina branding. All placeholder pages have professional empty states. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of nautical theme and role layouts</name>
  <files>none -- verification only</files>
  <action>
    Human verifies the visual output of Tasks 1 and 2. No code changes needed.
    What was built:
    1. Ocean/navy/teal/seafoam color palette (not default zinc/gray)
    2. Admin sidebar with deep navy background, marina branding, navigation
    3. Staff sidebar with dock operations navigation
    4. Boater top navigation bar
    5. Styled login page with SlipSync branding and demo credentials
    6. Responsive behavior (sidebar collapses on tablet)
  </action>
  <verify>
    Human performs these steps:
    1. Run `npm run dev` and visit http://localhost:3000
    2. Verify login page: nautical colors (navy/teal/seafoam), SlipSync branding, demo credentials shown
    3. Log in as admin@slipsync.app / demo1234 -> verify deep navy sidebar, Dashboard heading, navigation links
    4. Log out, log in as dock@slipsync.app / demo1234 -> verify staff sidebar with operations navigation
    5. Log out, log in as boater@slipsync.app / demo1234 -> verify top navigation bar (not sidebar)
    6. Resize browser to tablet width (~768px) -> verify sidebar collapses or hamburger menu appears
    7. Overall impression: does this look professional enough for LinkedIn?
  </verify>
  <done>Human confirms nautical theme looks professional, all three role layouts render correctly, responsive behavior works on tablet. Approved for LinkedIn showcase quality.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx vitest run --reporter=verbose` -- all existing tests still pass
- Visual: Login page shows nautical theme with SlipSync branding
- Visual: Admin layout has deep navy sidebar with navigation
- Visual: Boater layout has top navigation bar
- Visual: Responsive -- sidebar collapses on tablet viewport
</verification>

<success_criteria>
- Application uses navy/teal/seafoam color palette throughout (not default shadcn/ui zinc)
- Admin and staff see sidebar navigation with marina branding
- Boater sees top navigation bar
- Login page is visually polished with branding and demo credential hints
- All three demo accounts land on correct pages after login
- Sidebar collapses on tablet screens
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md`
</output>
