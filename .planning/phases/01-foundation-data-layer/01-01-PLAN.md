---
phase: 01-foundation-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - drizzle.config.ts
  - .env.local
  - .env.example
  - .gitignore
  - src/db/index.ts
  - src/db/schema.ts
  - src/db/seed.ts
  - src/db/seed-helpers.ts
  - vitest.config.ts
  - src/__tests__/schema.test.ts
  - src/__tests__/seed.test.ts
autonomous: true
requirements:
  - SLIP-01
  - SLIP-02
  - SEED-01
  - SEED-02
  - SEED-03
  - SEED-04
  - SEED-05
  - AUTH-05

must_haves:
  truths:
    - "Database contains 4 docks (A, B, C, D) with 20, 15, 15, 10 slips respectively totaling 60"
    - "Each slip has max_length, max_beam, max_draft dimensions appropriate to its dock size category"
    - "Three demo accounts exist with bcrypt-hashed passwords: admin@slipsync.app, dock@slipsync.app, boater@slipsync.app"
    - "15-20 boater accounts exist with at least one vessel each"
    - "Slip status distribution is approximately 40% occupied, 20% reserved, 30% available, 10% maintenance"
    - "Historical bookings use relative dates computed from Date.now(), not hardcoded date strings"
    - "Waitlist entries and maintenance requests exist in various states"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "All table definitions: users, docks, slips, vessels, bookings, waitlist, maintenanceRequests with relations"
      exports: ["users", "docks", "slips", "vessels", "bookings", "waitlist", "maintenanceRequests"]
    - path: "src/db/index.ts"
      provides: "Single database connection instance"
      exports: ["db"]
    - path: "src/db/seed.ts"
      provides: "Complete seed script populating all demo data"
    - path: "src/db/seed-helpers.ts"
      provides: "daysFromNow() and isoNow() helpers for relative dates"
      exports: ["daysFromNow", "isoNow"]
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit configuration for SQLite"
  key_links:
    - from: "src/db/seed.ts"
      to: "src/db/schema.ts"
      via: "imports all table definitions for insert operations"
      pattern: "import.*from.*schema"
    - from: "src/db/seed.ts"
      to: "src/db/seed-helpers.ts"
      via: "uses daysFromNow() for all date values"
      pattern: "daysFromNow\\("
    - from: "src/db/index.ts"
      to: "src/db/schema.ts"
      via: "passes schema to drizzle() for typed queries"
      pattern: "drizzle\\(sqlite.*schema"
---

<objective>
Scaffold the Next.js project, define the complete Drizzle SQLite schema, and create a comprehensive seed script that populates "Sunset Harbor Marina" with realistic demo data.

Purpose: Establish the data foundation that all subsequent plans and phases build upon. Without the schema and seed data, auth cannot verify users and layouts have nothing to display.
Output: Running Next.js app with SQLite database, all tables created via drizzle-kit push, and seed script that populates 60 slips, 20+ users, vessels, bookings, waitlist, and maintenance requests.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project, install all Phase 1 dependencies, create database schema and connection</name>
  <files>
    package.json, tsconfig.json, next.config.ts, drizzle.config.ts, .env.local, .env.example, .gitignore,
    src/db/index.ts, src/db/schema.ts, src/db/seed-helpers.ts,
    vitest.config.ts, src/__tests__/schema.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-data-layer/01-RESEARCH.md
    .planning/phases/01-foundation-data-layer/01-CONTEXT.md
    BRIEF.md
  </read_first>
  <action>
    1. Initialize Next.js project (if not already initialized):
       ```
       npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --turbopack
       ```
       If project already exists, skip this step.

    2. Install all Phase 1 dependencies:
       ```
       npm install drizzle-orm better-sqlite3 iron-session bcryptjs zod react-hook-form @hookform/resolvers date-fns lucide-react
       npm install -D drizzle-kit @types/better-sqlite3 @types/bcryptjs vitest @vitejs/plugin-react tsx
       ```

    3. Create `.env.local` with:
       ```
       SESSION_SECRET=this-is-a-development-secret-that-is-at-least-32-characters-long
       DATABASE_URL=./data/slipsync.db
       ```

    4. Create `.env.example` with same keys but placeholder values:
       ```
       SESSION_SECRET=your-secret-here-must-be-at-least-32-characters
       DATABASE_URL=./data/slipsync.db
       ```

    5. Add to `.gitignore`: `data/` directory for SQLite files.

    6. Create `drizzle.config.ts`:
       ```typescript
       import { defineConfig } from 'drizzle-kit';
       export default defineConfig({
         out: './drizzle',
         schema: './src/db/schema.ts',
         dialect: 'sqlite',
         dbCredentials: { url: './data/slipsync.db' },
       });
       ```

    7. Create `src/db/index.ts` -- Single database connection:
       ```typescript
       import Database from 'better-sqlite3';
       import { drizzle } from 'drizzle-orm/better-sqlite3';
       import * as schema from './schema';
       import path from 'path';
       import fs from 'fs';

       const dataDir = path.join(process.cwd(), 'data');
       if (!fs.existsSync(dataDir)) fs.mkdirSync(dataDir, { recursive: true });

       const dbPath = path.join(dataDir, 'slipsync.db');
       const sqlite = new Database(dbPath);
       sqlite.pragma('journal_mode = WAL');
       sqlite.pragma('foreign_keys = ON');

       export const db = drizzle(sqlite, { schema });
       ```

    8. Create `src/db/schema.ts` with ALL tables exactly as specified in RESEARCH.md Pattern 3:
       - `users` table: id (integer PK autoIncrement), email (text unique notNull), passwordHash (text notNull), name (text notNull), role (text enum ['admin','dock_staff','boater'] notNull), phone (text), createdAt (text notNull with $defaultFn ISO string)
       - `docks` table: id (integer PK autoIncrement), name (text notNull), description (text), sizeCategory (text enum ['small','medium','large','extra_large'] notNull), minLength (real notNull), maxLength (real notNull)
       - `slips` table: id (integer PK autoIncrement), dockId (integer FK->docks.id notNull), number (text unique notNull), maxLength (real notNull), maxBeam (real notNull), maxDraft (real notNull), status (text enum ['available','occupied','reserved','maintenance'] default 'available' notNull), priceDaily (real notNull), priceMonthly (real notNull), hasElectric (integer boolean default true notNull), hasWater (integer boolean default true notNull), notes (text)
       - `vessels` table: id (integer PK autoIncrement), ownerId (integer FK->users.id notNull), name (text notNull), type (text notNull), loa (real notNull), beam (real notNull), draft (real notNull), registrationNumber (text), year (integer), createdAt (text notNull)
       - `bookings` table: id (integer PK autoIncrement), slipId (integer FK->slips.id notNull), vesselId (integer FK->vessels.id notNull), boaterId (integer FK->users.id notNull), type (text enum ['transient','seasonal'] notNull), status (text enum ['pending','confirmed','checked_in','checked_out','cancelled'] default 'pending' notNull), startDate (text notNull), endDate (text notNull), totalPrice (real notNull), notes (text), createdAt (text notNull), updatedAt (text notNull)
       - `waitlist` table: id (integer PK autoIncrement), boaterId (integer FK->users.id notNull), vesselId (integer FK->vessels.id notNull), preferredDock (text), minLength (real), requestedStart (text), requestedEnd (text), status (text enum ['waiting','offered','accepted','expired'] default 'waiting' notNull), priority (integer default 0 notNull), createdAt (text notNull)
       - `maintenanceRequests` table: id (integer PK autoIncrement), slipId (integer FK->slips.id notNull), reportedBy (integer FK->users.id notNull), assignedTo (integer FK->users.id), title (text notNull), description (text notNull), priority (text enum ['low','medium','high','urgent'] default 'medium' notNull), status (text enum ['open','in_progress','completed','cancelled'] default 'open' notNull), createdAt (text notNull), resolvedAt (text)
       - ALL relations as shown in RESEARCH.md Pattern 3 (usersRelations, docksRelations, slipsRelations, vesselsRelations, bookingsRelations)

    9. Create `src/db/seed-helpers.ts`:
       ```typescript
       import { addDays, subDays, format } from 'date-fns';
       const now = new Date();
       export function daysFromNow(offset: number): string {
         const date = offset >= 0 ? addDays(now, offset) : subDays(now, Math.abs(offset));
         return format(date, 'yyyy-MM-dd');
       }
       export function isoNow(): string {
         return now.toISOString();
       }
       ```

    10. Create `vitest.config.ts`:
        ```typescript
        import { defineConfig } from 'vitest/config';
        import path from 'path';
        export default defineConfig({
          test: {
            globals: true,
            environment: 'node',
          },
          resolve: {
            alias: { '@': path.resolve(__dirname, './src') },
          },
        });
        ```

    11. Create `src/__tests__/schema.test.ts` with test stubs:
        - Test "slip dimensions": Import slips from schema, verify it has maxLength, maxBeam, maxDraft columns
        - Test "dock categories": Import docks from schema, verify sizeCategory enum includes 'small','medium','large','extra_large'
        - These are schema structure tests that verify the table definitions exist with correct column names

    12. Run `mkdir -p data && npx drizzle-kit push` to create the database tables.

    13. Add a `"db:seed"` script to package.json: `"db:seed": "tsx src/db/seed.ts"`
    14. Add a `"db:push"` script to package.json: `"db:push": "drizzle-kit push"`
    15. Add a `"test"` script to package.json: `"test": "vitest run --reporter=verbose"`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/schema.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.ts contains `export const users = sqliteTable`
    - src/db/schema.ts contains `export const docks = sqliteTable`
    - src/db/schema.ts contains `export const slips = sqliteTable`
    - src/db/schema.ts contains `export const vessels = sqliteTable`
    - src/db/schema.ts contains `export const bookings = sqliteTable`
    - src/db/schema.ts contains `export const waitlist = sqliteTable`
    - src/db/schema.ts contains `export const maintenanceRequests = sqliteTable`
    - src/db/schema.ts contains `maxLength: real('max_length')`
    - src/db/schema.ts contains `maxBeam: real('max_beam')`
    - src/db/schema.ts contains `maxDraft: real('max_draft')`
    - src/db/index.ts contains `export const db = drizzle(`
    - src/db/seed-helpers.ts contains `export function daysFromNow(`
    - drizzle.config.ts contains `dialect: 'sqlite'`
    - .env.local contains `SESSION_SECRET=`
    - .env.local contains `DATABASE_URL=`
    - package.json contains `"db:seed"`
    - package.json contains `"test"`
    - vitest.config.ts exists and contains `defineConfig`
    - data/slipsync.db file exists (created by drizzle-kit push)
    - `npx vitest run src/__tests__/schema.test.ts` exits 0
  </acceptance_criteria>
  <done>Next.js project scaffolded with all Phase 1 deps installed. Drizzle schema defines all 7 tables with relations. Database file exists with tables created. Schema tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive seed script and seed data tests</name>
  <files>
    src/db/seed.ts, src/__tests__/seed.test.ts
  </files>
  <read_first>
    src/db/schema.ts
    src/db/index.ts
    src/db/seed-helpers.ts
    .planning/phases/01-foundation-data-layer/01-RESEARCH.md
    .planning/phases/01-foundation-data-layer/01-CONTEXT.md
    BRIEF.md
  </read_first>
  <action>
    1. Create `src/__tests__/seed.test.ts` with these test cases (write tests FIRST):
       - "demo accounts": After seeding, db should contain users with emails admin@slipsync.app, dock@slipsync.app, boater@slipsync.app. Verify bcrypt hashes validate against "demo1234".
       - "marina structure": After seeding, db should contain exactly 4 docks (A, B, C, D) and exactly 60 slips (20+15+15+10).
       - "boaters": After seeding, db should contain between 15 and 20 users with role='boater', each having at least 1 vessel.
       - "status distribution": After seeding, slip statuses should be approximately 40% occupied (22-26), 20% reserved (10-14), 30% available (16-20), 10% maintenance (4-8).
       - "relative dates": After seeding, no booking startDate or endDate should be a hardcoded string. Verify all dates are within 180 days of today (3 months back + 3 months forward).
       - "waitlist and maintenance": After seeding, waitlist table should have at least 3 entries, maintenanceRequests should have at least 4 entries in mixed statuses (at least one 'open', one 'in_progress', one 'completed').
       - "boater demo account has vessels": boater@slipsync.app should have exactly 2 vessels and at least 1 active booking (status 'confirmed' or 'checked_in').

       Each test should: import db, import schema tables, run seed function, query the database, assert. Use a beforeAll that runs the seed on a fresh test database. Use a separate test DB path (e.g., data/test-slipsync.db) to avoid conflicts.

    2. Create `src/db/seed.ts`:
       - Export a `seed()` function AND run it directly when file is executed (`if (require.main === module)` or top-level await pattern)
       - Clear all tables before seeding (delete in reverse FK order: maintenanceRequests, waitlist, bookings, vessels, slips, docks, users)
       - Use `daysFromNow()` from seed-helpers for ALL dates. No hardcoded date strings.
       - Use `bcryptjs.hashSync('demo1234', 10)` for all passwords.

       **Docks** (4 docks):
       - Dock A: name='A', description='Small craft berths', sizeCategory='small', minLength=20, maxLength=30
       - Dock B: name='B', description='Mid-size vessel berths', sizeCategory='medium', minLength=30, maxLength=45
       - Dock C: name='C', description='Large vessel berths', sizeCategory='large', minLength=45, maxLength=65
       - Dock D: name='D', description='Mega yacht berths', sizeCategory='extra_large', minLength=65, maxLength=100

       **Slips** (60 total):
       - Dock A: 20 slips (A-01 through A-20), maxLength 22-30ft graduating, maxBeam 8-10ft, maxDraft 3-5ft, priceDaily $45-65, priceMonthly $800-1200
       - Dock B: 15 slips (B-01 through B-15), maxLength 32-45ft, maxBeam 12-14ft, maxDraft 5-7ft, priceDaily $75-110, priceMonthly $1400-2200
       - Dock C: 15 slips (C-01 through C-15), maxLength 48-65ft, maxBeam 16-18ft, maxDraft 6-9ft, priceDaily $130-200, priceMonthly $2800-4500
       - Dock D: 10 slips (D-01 through D-10), maxLength 68-100ft, maxBeam 20-26ft, maxDraft 8-12ft, priceDaily $250-450, priceMonthly $5500-9000
       - Status distribution across all 60: ~24 occupied, ~12 reserved, ~18 available, ~6 maintenance. Assign deterministically (e.g., first N of each dock get a specific status).

       **Users** (3 staff + 18 boaters = 21 total):
       - admin@slipsync.app, name='Harbor Master', role='admin'
       - dock@slipsync.app, name='Jake Martinez', role='dock_staff'
       - boater@slipsync.app, name='Sarah Chen', role='boater'
       - 15 additional boater accounts with realistic names like: 'Mike Thompson', 'Lisa Rodriguez', 'Tom Wilson', 'Jennifer Park', 'David Kim', 'Amanda Foster', 'Robert Hayes', 'Emily Watson', 'James Mitchell', 'Maria Santos', 'Chris Anderson', 'Nicole Taylor', 'Steven Clark', 'Rachel Green', 'Daniel Brooks'

       **Vessels** (25-30 total):
       - boater@slipsync.app (Sarah Chen) gets exactly 2 vessels:
         1. "Sea Breeze" sailboat, LOA 32ft, beam 11ft, draft 5.5ft, year 2019
         2. "Harbor Light" powerboat, LOA 24ft, beam 8.5ft, draft 3ft, year 2022
       - Each other boater gets 1-2 vessels with nautical names: "Windward Spirit", "Blue Horizon", "Ocean Pearl", "Starboard Dream", "Tidal Wave", "Coral Runner", "Pacific Wanderer", "Moonlit Bay", "Silver Wake", "Coastal Gem", "Sunset Chaser", "Anchor's Rest", "Deep Current", "Marina Breeze", "Wave Dancer"
       - Vessel types: mix of 'sailboat', 'powerboat', 'catamaran', 'trawler', 'center_console'
       - Dimensions must fit within the slip they will be booked into (vessel LOA < slip maxLength, etc.)

       **Bookings** (~35-40 total):
       - Current active bookings: for all 'occupied' slips, create a booking with status='checked_in', startDate=daysFromNow(-random 3-30), endDate=daysFromNow(+random 5-30)
       - Upcoming reservations: for all 'reserved' slips, create a booking with status='confirmed', startDate=daysFromNow(+random 1-14), endDate=daysFromNow(+random 15-45)
       - Historical bookings: 10-15 past bookings with status='checked_out', dates spanning daysFromNow(-90) to daysFromNow(-7)
       - Sarah Chen (boater@slipsync.app) must have 1 active booking (checked_in) with her "Sea Breeze" vessel
       - Booking types: mix of 'transient' and 'seasonal'. Seasonal bookings have longer date ranges (60-180 days).
       - totalPrice calculated from slip daily rate * number of days

       **Waitlist** (4-5 entries):
       - Mix of statuses: 2 'waiting', 1 'offered', 1 'expired'
       - Different boaters requesting preferred docks
       - requestedStart/End using daysFromNow() for future dates

       **Maintenance Requests** (5-6 entries):
       - 2 'open' (recently reported)
       - 2 'in_progress' (assigned to dock staff)
       - 1 'completed' (with resolvedAt date)
       - 1 'cancelled'
       - Mix of priorities: low, medium, high, urgent
       - Reported by various boaters, assigned to dock@slipsync.app staff user
       - Titles like: "Dock cleat loose", "Power outlet not working", "Water leak at slip", "Bumper replacement needed", "Light fixture broken", "Dock board cracked"

    3. Run `npm run db:seed` to verify seed completes without errors.
    4. Run tests: `npx vitest run src/__tests__/seed.test.ts --reporter=verbose`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/seed.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/db/seed.ts contains `export async function seed(` or `export function seed(`
    - src/db/seed.ts contains `daysFromNow(` (at least 5 occurrences -- no hardcoded dates)
    - src/db/seed.ts contains `bcrypt` or `bcryptjs` for password hashing
    - src/db/seed.ts contains `admin@slipsync.app`
    - src/db/seed.ts contains `dock@slipsync.app`
    - src/db/seed.ts contains `boater@slipsync.app`
    - src/db/seed.ts does NOT contain a date string matching pattern `202[0-9]-[0-9]{2}-[0-9]{2}` (no hardcoded dates)
    - src/__tests__/seed.test.ts contains `demo accounts`
    - src/__tests__/seed.test.ts contains `marina structure`
    - src/__tests__/seed.test.ts contains `status distribution`
    - src/__tests__/seed.test.ts contains `relative dates`
    - `npx vitest run src/__tests__/seed.test.ts` exits 0
    - `npm run db:seed` exits 0
  </acceptance_criteria>
  <done>Seed script populates Sunset Harbor Marina with 4 docks, 60 slips, 21 users (3 staff + 18 boaters), 25+ vessels, 35+ bookings, waitlist entries, and maintenance requests. All dates are relative. All seed tests pass. Sarah Chen has 2 vessels and 1 active booking.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all schema and seed tests pass
- `npm run db:seed` completes without errors
- `sqlite3 data/slipsync.db "SELECT COUNT(*) FROM slips"` returns 60
- `sqlite3 data/slipsync.db "SELECT COUNT(*) FROM users"` returns 21
- `npm run build` succeeds (Next.js compiles without errors)
</verification>

<success_criteria>
- All 7 database tables exist with correct columns and relations
- Seed script creates exactly 60 slips across 4 docks with correct distributions
- Three demo accounts work with password "demo1234"
- All dates in seed are relative (no hardcoded date strings)
- Both schema and seed test suites pass green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-layer/01-01-SUMMARY.md`
</output>
