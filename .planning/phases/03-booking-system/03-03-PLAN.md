---
phase: 03-booking-system
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/booking/booking-calendar.tsx
  - src/components/booking/booking-bar.tsx
  - src/components/booking/arrivals-departures.tsx
  - src/app/(admin)/bookings/calendar/page.tsx
  - src/app/(staff)/operations/page.tsx
autonomous: true
requirements: [BOOK-06, BOOK-07]

must_haves:
  truths:
    - "Calendar view shows monthly grid with slip rows and day columns for a selected dock"
    - "Booking bars span across days showing vessel name and are color-coded by status"
    - "Dock tabs allow switching between Dock A, B, C, D"
    - "Month navigation arrows move between months"
    - "Clicking an empty day cell navigates to booking form pre-filled with slip and date"
    - "Today's arrivals (confirmed bookings starting today) displayed as actionable cards"
    - "Today's departures (checked-in bookings ending today) displayed as actionable cards"
    - "Arrival cards have Check In button, departure cards have Check Out button"
  artifacts:
    - path: "src/components/booking/booking-calendar.tsx"
      provides: "Gantt-style monthly calendar grid per dock"
      contains: "eachDayOfInterval"
    - path: "src/components/booking/booking-bar.tsx"
      provides: "Horizontal booking bar spanning days on calendar"
      contains: "booking"
    - path: "src/components/booking/arrivals-departures.tsx"
      provides: "Card list of today's arrivals and departures with action buttons"
      contains: "getTodayArrivals"
    - path: "src/app/(admin)/bookings/calendar/page.tsx"
      provides: "Calendar page with dock tabs and month navigation"
      contains: "BookingCalendar"
    - path: "src/app/(staff)/operations/page.tsx"
      provides: "Staff operations page with arrivals/departures"
      contains: "ArrivalsAndDepartures"
  key_links:
    - from: "src/components/booking/booking-calendar.tsx"
      to: "src/lib/dal/bookings.ts"
      via: "receives bookings data from server component"
      pattern: "getBookingsForDockInMonth"
    - from: "src/components/booking/arrivals-departures.tsx"
      to: "src/app/actions/booking-actions.ts"
      via: "calls updateBookingStatus for check-in/check-out"
      pattern: "updateBookingStatus"
---

<objective>
Build the calendar availability view (Gantt-style monthly grid per dock) and the arrivals/departures card list for staff operations.

Purpose: Gives staff visual oversight of all bookings across time and enables quick check-in/check-out from today's arrivals and departures.
Output: Calendar page with dock tabs and booking bars, arrivals/departures component on staff operations page.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-booking-system/03-CONTEXT.md
@.planning/phases/03-booking-system/03-RESEARCH.md
@.planning/phases/03-booking-system/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/dal/bookings.ts:
```typescript
export async function getBookingsForDockInMonth(dockId: number, year: number, month: number): Promise<BookingWithRelations[]>;
export async function getTodayArrivals(): Promise<BookingWithRelations[]>;
export async function getTodayDepartures(): Promise<BookingWithRelations[]>;
export async function getUpcomingArrivalsAndDepartures(days?: number): Promise<{arrivals: BookingWithRelations[], departures: BookingWithRelations[]}>;
```

From src/app/actions/booking-actions.ts:
```typescript
export async function updateBookingStatus(bookingId: number, newStatus: string): Promise<{success: boolean; error?: string}>;
```

From src/db/schema.ts:
```typescript
// bookings.status: 'pending' | 'confirmed' | 'checked_in' | 'checked_out' | 'cancelled'
// docks: id, name (A/B/C/D), sizeCategory
// slips: id, dockId, number
```

From src/lib/domain/booking-rules.ts:
```typescript
export function canTransition(from: string, to: string): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calendar availability view with Gantt-style booking bars</name>
  <files>src/components/booking/booking-calendar.tsx, src/components/booking/booking-bar.tsx, src/app/(admin)/bookings/calendar/page.tsx</files>
  <read_first>src/lib/dal/bookings.ts, src/db/schema.ts, .planning/phases/03-booking-system/03-CONTEXT.md, .planning/phases/03-booking-system/03-RESEARCH.md, src/app/(admin)/layout.tsx</read_first>
  <action>
    1. Create `src/components/booking/booking-bar.tsx` as a 'use client' component:
       - Props: `booking` (with vessel, slip, boater), `monthStart: Date`, `daysInMonth: number`, `onBookingClick: (booking) => void`
       - Calculates bar position: startCol = max(1, differenceInDays(bookingStart, monthStart) + 1), endCol = min(daysInMonth + 1, differenceInDays(bookingEnd, monthStart) + 1). Use date-fns differenceInDays, parseISO.
       - Renders as a div with absolute positioning within the grid row, using CSS grid column start/end.
       - Color-coded by status per user decision: pending = amber/yellow bg, confirmed = blue bg, checked_in = green bg, checked_out = gray bg, cancelled = red bg with opacity.
       - Shows vessel name (truncated with text-ellipsis if needed).
       - Rounded corners on visible edges (left-rounded if booking starts in this month, right-rounded if ends in this month).
       - Clickable -- calls onBookingClick.
       - Height: ~24px, vertically centered in row.

    2. Create `src/components/booking/booking-calendar.tsx` as a 'use client' component:
       - Props: `docks` (array of dock objects), `initialDockId: number`, `initialYear: number`, `initialMonth: number`, `bookingsByDock: Record<number, BookingWithSlipAndVessel[]>`, `slipsByDock: Record<number, Slip[]>`
       - Uses useState for selectedDockId, currentYear, currentMonth.
       - **Dock tabs**: Tabs component (or custom buttons) for Dock A, B, C, D. Styled as pill buttons.
       - **Month navigation**: Left arrow, "March 2026" label (format with date-fns), Right arrow. Uses router.push or client-side state to change month. When month changes, trigger re-fetch via URL params: `/admin/bookings/calendar?dockId=1&year=2026&month=3`.
       - **Calendar grid**: CSS grid layout.
         - Header row: day numbers 1-31 (or however many days in month). Use `eachDayOfInterval({start: startOfMonth, end: endOfMonth})` from date-fns. Weekend columns slightly darker bg.
         - One row per slip in the selected dock. Row label = slip number (e.g., "A-01") in a fixed first column.
         - Each day cell is clickable if no booking bar covers it. On click, navigate to `/admin/bookings/new?slipId={slipId}&date={YYYY-MM-DD}`.
         - BookingBar components positioned within rows using CSS grid column placement.
       - Today's column highlighted with a subtle vertical line or different bg color.
       - Compact: rows ~32px height, day columns ~40px min width. Horizontal scroll if needed.
       - Use shadcn Tabs for dock selection, Button for navigation arrows.

    3. Create `src/app/(admin)/bookings/calendar/page.tsx` as a server component:
       - Read searchParams for dockId (default: first dock), year (default: current year), month (default: current month)
       - Fetch all docks from DB
       - Fetch slips for selected dock
       - Fetch bookings for selected dock and month using getBookingsForDockInMonth()
       - Also fetch bookings for all docks in current month (for dock tab counts, optional)
       - Pass data to BookingCalendar client component
       - Page title: "Booking Calendar"

    4. Add "Calendar" link to admin sidebar navigation alongside the "Bookings" link added in Plan 02. Use Calendar icon from lucide-react, pointing to /admin/bookings/calendar.
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/slipsync && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/components/booking/booking-calendar.tsx
    - grep -q "eachDayOfInterval" src/components/booking/booking-calendar.tsx
    - grep -q "use client" src/components/booking/booking-bar.tsx
    - grep -q "differenceInDays" src/components/booking/booking-bar.tsx
    - grep -q "getBookingsForDockInMonth" src/app/\(admin\)/bookings/calendar/page.tsx
    - grep -q "BookingCalendar" src/app/\(admin\)/bookings/calendar/page.tsx
    - Build succeeds
  </acceptance_criteria>
  <done>Calendar page renders monthly Gantt grid with dock tabs, month navigation, slip rows, day columns, color-coded booking bars, and clickable empty cells that link to booking form. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Arrivals and departures cards on staff operations page</name>
  <files>src/components/booking/arrivals-departures.tsx, src/app/(staff)/operations/page.tsx</files>
  <read_first>src/app/(staff)/operations/page.tsx, src/app/(staff)/layout.tsx, src/lib/dal/bookings.ts, src/app/actions/booking-actions.ts, .planning/phases/03-booking-system/03-CONTEXT.md</read_first>
  <action>
    1. Create `src/components/booking/arrivals-departures.tsx` as a 'use client' component:
       - Props: `arrivals` (array of bookings with slip+dock, vessel, boater), `departures` (same type)
       - Two sections side-by-side (grid cols 2 on desktop, stacked on mobile):
         a. **Today's Arrivals** header with Ship icon and count badge
         b. **Today's Departures** header with Anchor icon and count badge
       - Each section renders cards. Each card shows:
         - Slip number + dock name (e.g., "A-05, Dock A") as card header
         - Vessel name + type (e.g., "Sea Breeze (Sailboat)")
         - Boater name
         - Date range (formatted: "Mar 10 - Mar 17, 2026")
         - **Action button**: Arrivals get "Check In" button (calls updateBookingStatus(id, 'checked_in')), Departures get "Check Out" button (calls updateBookingStatus(id, 'checked_out'))
         - Check Out button uses AlertDialog for confirmation (per user decision)
         - Check In button calls directly (low-risk per user decision)
         - Loading state on button while action is pending (useTransition)
         - On success: button changes to success state briefly, then card updates
       - Empty state: "No arrivals today" / "No departures today" with appropriate icon
       - Use shadcn Card, Button, Badge, AlertDialog
       - Responsive: 2 columns on md+, 1 column on mobile

    2. Update `src/app/(staff)/operations/page.tsx`:
       - This is a server component. Read existing content first.
       - Import and call getTodayArrivals() and getTodayDepartures() from DAL
       - Render ArrivalsAndDepartures component with the data
       - Keep any existing content on the page, add arrivals/departures as the primary content section
       - Page title: "Operations" (likely already set)
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/slipsync && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/components/booking/arrivals-departures.tsx
    - grep -q "updateBookingStatus" src/components/booking/arrivals-departures.tsx
    - grep -q "Check In" src/components/booking/arrivals-departures.tsx
    - grep -q "Check Out" src/components/booking/arrivals-departures.tsx
    - grep -q "AlertDialog" src/components/booking/arrivals-departures.tsx
    - grep -q "getTodayArrivals" src/app/\(staff\)/operations/page.tsx
    - grep -q "getTodayDepartures" src/app/\(staff\)/operations/page.tsx
    - grep -q "ArrivalsAndDepartures" src/app/\(staff\)/operations/page.tsx
    - Build succeeds
  </acceptance_criteria>
  <done>Staff operations page shows today's arrivals with Check In buttons and today's departures with Check Out buttons (with confirmation). Empty states handled. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- Calendar page renders at /admin/bookings/calendar
- Calendar shows dock tabs, month navigation, slip rows with booking bars
- Empty day cells link to /admin/bookings/new?slipId=X&date=YYYY-MM-DD
- Staff operations page at /staff/operations shows arrivals and departures
- Action buttons call updateBookingStatus correctly
- `npx vitest run --reporter=verbose` -- all existing tests pass
</verification>

<success_criteria>
- Calendar view displays Gantt-style booking bars per dock per month
- Bars are color-coded by booking status (pending=yellow, confirmed=blue, checked_in=green, checked_out=gray)
- Dock tabs and month navigation work
- Clicking empty day navigates to pre-filled booking form
- Today's arrivals and departures display on staff operations page with action buttons
- Full test suite and build pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-system/03-03-SUMMARY.md`
</output>
